<div class="fields">

  <%= f.label :content, "Question" %><br />
  <%= link_to_remove_fields "remove", f %>
  <%= f.text_area :content, :rows => 3 %><br />

<%= f.fields_for :options do |builder| %>
  <%= render 'option_fields', :f => builder %>
  
<% end %>
<p><%= link_to_add_fields "Add Answer", f, :options %></p>

</div>