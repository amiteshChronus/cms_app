
<li>
  <%= gravatar_for user, size: 40 %>
  <%= link_to user.name, user ,  :class=> "forget_password" %>
  <% if isAdmin? %>
    | <%= link_to "delete",  user,:class=> "forget_password", method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
