<% provide(:title, @title) %>
<div class="span8">
    <h3><%= @title %></h3>
    <%= link_to "New Quiz", new_quiz_path(:course_id => @course.id), class: "btn btn-large btn-primary"%>
    
    <% if @quizzes.any? %>
      

      <ul class="quiz">

       	 <%= render :partial => "quizzes/show" %>
      </ul>
      
    <% end %>
  </div>