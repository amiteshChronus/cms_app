<% provide(:title, @title) %>
<div class="span8">
    <h3><%= @title %></h3>
    <% if @students.any? %>
      <ul class="users">
        <%= render @students %>
      </ul>
      
    <% end %>
  </div>